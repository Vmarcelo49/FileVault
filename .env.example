PORT=3000
SHARED_DIR=./shared
SUBDOMAIN=
LOCAL_ONLY=false
TUNNEL_PROVIDER=cloudflare
AUTH_TOKEN=

# HeadlessLab integration (optional — for live view feature)
# Path to the headless CLI binary. Default: /home/z/bin/headless
# HEADLESS_BIN=/home/z/bin/headless
# Set to 'false' to disable the live view feature entirely
# HEADLESS_MONITOR_ENABLED=true
